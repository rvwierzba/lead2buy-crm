services:
  - type: web
    name: lead2buy-api
    plan: free
    env: docker
    dockerfilePath: ./Lead2Buy.API/Dockerfile
    envVars:
      # ... (suas variáveis de ambiente, que já estão corretas)
  - type: web
    name: lead2buy-ollama
    plan: free
    env: docker
    dockerfilePath: ./ollama/Dockerfile
  - type: web
    name: lead2buy-frontend
    runtime: static
    # ... (resto da configuração do frontend)
  - type: redis
    name: lead2buy-redis
    plan: free
    ipAllowList: []
databases:
  - name: lead2buy-postgres
    plan: free